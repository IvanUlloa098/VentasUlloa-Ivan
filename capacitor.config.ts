import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'VentasUlloa-Ivan',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
